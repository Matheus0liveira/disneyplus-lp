import styled from "styled-components";

export const Container = styled.footer`
  position: absolute;
  bottom: 32px;
  left: 32px;

  h4 {
    color: #6421ff;

    cursor: pointer;

    &:hover {
      filter: brightness(1.2);
    }
  }
`;
